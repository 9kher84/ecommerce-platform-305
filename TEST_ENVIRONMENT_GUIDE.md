# 🧪 دليل بيئة التجربة

## ✅ تم الإعداد بنجاح!

تم إنشاء بيئة تجريبية كاملة مع مستخدمين وبيانات جاهزة للاختبار.

---

## 📋 الحسابات المتوفرة

### ✅ تم التحقق من جميع كلمات المرور - جاهزة للاستخدام!

### كلمة المرور لجميع الحسابات: `123456`

| الدور | البريد الإلكتروني | الصلاحيات | الباقة | الحالة |
|------|-------------------|-----------|---------|---------|
| 👑 **المالك** | `owner@test.com` | كاملة (Full Access) | Plan B | ✅ نشط |
| 🔧 **مسؤول** | `admin@test.com` | محدودة (Users + System) | Plan B | ✅ نشط |
| 🏪 **بائع** | `seller@test.com` | بائع عادي | Plan A | ✅ نشط |
| 💎 **مشتري مميز** | `buyer-premium@test.com` | مشتري | Plan B | ✅ نشط |
| 👤 **مشتري عادي** | `buyer@test.com` | مشتري | Free | ✅ نشط |

**ملاحظة:** إذا واجهت مشكلة في تسجيل الدخول، شغل:
```bash
cd backend
node verify_passwords.js
```

---

## 🚀 كيفية البدء

### 1. تشغيل السيرفر

```bash
cd backend
npm start
```

**ملاحظة:** السيرفر سيعمل بدون Redis مع تحذيرات - هذا طبيعي!

### 2. تسجيل الدخول

استخدم أي من الحسابات أعلاه:
- البريد: `owner@test.com`
- كلمة المرور: `123456`

### 3. الوصول إلى لوحة التحكم الإدارية

بعد تسجيل الدخول كـ Owner أو Admin:
- انتقل إلى: `http://localhost:5000/admin`
- أو استخدم الشريط العائم (👑) في أسفل الشاشة

---

## 🎯 ما يمكنك تجربته

### كمالك (Owner)
✅ الوصول الكامل للوحة التحكم الإدارية
✅ إضافة/تعديل/حذف مسؤولين
✅ تنفيذ عمليات النظام (Backup, Maintenance)
✅ عرض السجلات (Logs)
✅ Impersonate (تسجيل دخول كمستخدم آخر)
✅ تعديل أي حقل في قاعدة البيانات

### كمسؤول (Admin)
✅ عرض المستخدمين
✅ تعديل بعض إعدادات المستخدمين
✅ عرض السجلات
⛔ لا يمكن إضافة مسؤولين جدد (Owner فقط)

### كبائع (Seller)
✅ إنشاء عروض أسعار
✅ إدارة المنتجات
✅ متابعة الصفقات

### كمشتري (Buyer)
✅ إنشاء طلبات شراء
✅ استقبال عروض الأسعار
✅ إتمام الصفقات

---

## 📊 البيانات المتوفرة

### التصنيفات
- إلكترونيات
- أثاث
- ملابس
- سيارات
- عقارات

### طلبات الشراء
- لابتوب للبرمجة (مشتري مميز)
- هاتف آيفون 15 (مشتري عادي)

---

## 🔧 الإعدادات المهمة

### ملف .env
تم تحديث ملف `.env` تلقائياً بـ:
```env
OWNER_ID=cf8b5bd6-f905-4456-afc1-2bd65902e27a
```

### قاعدة البيانات
- تم مزامنة جميع الجداول
- تم إضافة حقول Admin للمستخدمين
- البيانات جاهزة للاستخدام

---

## 🧪 سيناريوهات الاختبار المقترحة

### 1. اختبار النظام الإداري
```
1. سجل دخول كـ owner@test.com
2. انتقل إلى /admin
3. جرب:
   - عرض قائمة المسؤولين
   - Impersonate كمستخدم آخر
   - عرض السجلات
```

### 2. اختبار الصلاحيات
```
1. سجل دخول كـ admin@test.com
2. حاول الوصول إلى /admin
3. لاحظ الفرق في الصلاحيات
```

### 3. اختبار دورة الشراء
```
1. سجل دخول كـ buyer@test.com
2. أنشئ طلب شراء جديد
3. سجل خروج
4. سجل دخول كـ seller@test.com
5. قدم عرض سعر
6. ارجع للمشتري وقبل العرض
```

---

## ⚠️ ملاحظات مهمة

### Redis
- السيرفر يعمل بدون Redis
- ستظهر رسائل تحذير - تجاهلها
- لتفعيل Redis: `docker-compose up -d redis`

### قاعدة البيانات
- البيانات محفوظة في PostgreSQL
- لإعادة الإعداد: شغل `node setup_test_environment.js` مرة أخرى

### الأمان
- هذه بيئة تجريبية فقط
- كلمات المرور بسيطة للتجربة
- لا تستخدم هذه البيانات في الإنتاج

---

## 🔄 إعادة الإعداد

إذا أردت إعادة إنشاء البيئة من جديد:

```bash
cd backend
node setup_test_environment.js
```

سيقوم السكربت بـ:
- ✅ التحقق من الاتصال بقاعدة البيانات
- ✅ مزامنة الجداول
- ✅ إنشاء المستخدمين (أو استخدام الموجودين)
- ✅ إنشاء التصنيفات والطلبات

---

## 📞 المساعدة

إذا واجهت أي مشاكل:

1. **السيرفر لا يعمل:**
   - تحقق من تشغيل PostgreSQL
   - تحقق من ملف `.env`

2. **لا يمكن تسجيل الدخول:**
   - تأكد من البريد وكلمة المرور
   - شغل `setup_test_environment.js` مرة أخرى

3. **لوحة التحكم لا تظهر:**
   - تأكد من `OWNER_ID` في `.env`
   - تأكد من تسجيل الدخول كـ Owner

---

## 🎉 استمتع بالتجربة!

البيئة جاهزة تماماً للاختبار والتطوير. جرب جميع الميزات واكتشف النظام!
